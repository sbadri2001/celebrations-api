services:
  postgres:
    image: postgres:17
    container_name: celebrations-postgres

    environment:
      POSTGRES_DB: celebrations
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123

    ports:
      - '5432:5432'

    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: celebrations-redis

    ports:
      - '6379:6379'

  minio:
    image: minio/minio
    container_name: celebrations-minio

    command: server /data --console-address ":9001"

    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: admin123

    ports:
      - '9000:9000'
      - '9001:9001'

    volumes:
      - minio_data:/data

volumes:
  postgres_data:
  minio_data:
